import ButtonIcon from "../button";
import React, { FC } from "react";

const OptionsButton: FC = () => {
  return <ButtonIcon icon="bx-cog" text="Options" isCondensed="condensed" />;
};

export default OptionsButton;

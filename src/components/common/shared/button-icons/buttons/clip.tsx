import ButtonIcon from "../button-icon";
import React, { FC } from "react";

const ClipButton: FC = () => {
  return <ButtonIcon icon="bx bx-cut" textOn="Clip" hoverTextOn="Clip Video" />;
};

export default ClipButton;

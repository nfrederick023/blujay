import ButtonIcon from "../button-icon";
import React, { FC } from "react";

const VideoOptionsButton: FC = () => {
  return <ButtonIcon icon="bx bx-cog" hoverTextOn="Options" />;
};

export default VideoOptionsButton;
